10 reportes y 10 selects
Select
  cliente.ID
  cliente.nombre
  avg(cuenta.saldo) as Saldo
From clientes

5 funciones

combinar tablas:
inner join cuenta on

condicion normal:
where YEAR(cuenta.compra)=2018

agrupar datos si se usa f. agregada:
group by cliente.ID, cliente.nombre

condicion f. agregada:
having avg(cuenta.saldo)>100

1 select into, drop-insert into select
